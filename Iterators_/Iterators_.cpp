#include<iostream>
#include<iterator>
using namespace std;

int main() {
	//int arr[]{ 1, 98, 33, 99, 78 };
	//for (size_t i = 0; i < 5; i++)
	//{
	//	cout << arr[i] << ' ';
	//}
	//cout << '\n';

	//int* a = arr;
	///*cout << sizeof(arr) << endl;
	//cout << sizeof(a) << endl;*/

	//for (size_t i = 0; i < 5; i++)
	//{
	//	cout << a[i] << ' ';
	//}
	//
	//cout << '\n';

	//for (int item : arr) {
	//	item *= 10;
	//	cout << item << ' ';
	//}
	//cout << '\n';

	//for (int item : arr) {
	//	cout << item << ' ';
	//}
	//cout << '\n';

	/*
		input			==, !=, *i, i++, ++i, *i++
		output			++i, i++, *i = value, *i++ = value
		forward			==, !=, *i, ++i, i++, *i++
		bidirectional	==, !=, *i, ++i, i++, *i++, --i, i--, *i--
		random access	i += n, i + n, i -= n, i - n, i[n], i1 - i2,
						<, >, <=, >=, ==, !=, *i, ++i, i++, *i++, --i, i--, *i--
	
		advance(InIter &iter, Dist d)
		distance(InIter &start, InIter &start)
	*/


}